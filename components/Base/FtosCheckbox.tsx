import Checkbox, { CheckboxProps } from '@mui/material/Checkbox';
import React from 'react';

function FtosCheckbox(props: CheckboxProps) {
  return <Checkbox {...props} />;
}

export default FtosCheckbox;
