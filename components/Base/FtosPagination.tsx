import Pagination, { PaginationProps } from '@mui/material/Pagination';
import React from 'react';

function FtosPagination(props: PaginationProps) {
  return <Pagination {...props} />;
}

export default FtosPagination;
